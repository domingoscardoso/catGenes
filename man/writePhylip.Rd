% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writePhylip.R
\name{writePhylip}
\alias{writePhylip}
\title{Writes a phylip-formatted file of fully concatenated gene datasets and an
associated partition file for RAxML concatenated phylogenetic analysis}
\usage{
writePhylip(x,
           file,
           catalignments = TRUE,
           partitionfile = TRUE)
}
\arguments{
\item{x}{The object to be written, a list of equally-sized dataframes containing
the input gene datasets, as generated by the functions \\code{\link{catfullGenes}}
or \code{\link{catmultGenes}}.}

\item{file}{Either a character string naming a file or a \code{\link{connection}}
open for writing.}

\item{catalignments}{Logical, if \code{FALSE} will not write the concatednated
matrix of DNA alinments. Better to keep it always as \code{TRUE}.}

\item{partitionfile}{Logical, if \code{FALSE} will not write the associated
text-formatted partition file for the RAxML concatenated phylogenetic analysis
using a mixed/partitioned model.}
}
\description{
from the resulting list of equally-sized dataframes containing the
input gene datasets, as generated by the functions \code{\link{catfullGenes}}
or \code{\link{catmultGenes}}, it will write (i) a final phylip-formatted file
of fully concatenated gene datasets and (ii) an associated partition file for
the RAxML concatenated phylogenetic analysis using a mixed/partitioned model.
}
\examples{
\dontrun{
data(DNAlignments)
datasets4comb <- catfullGenes(DNAlignments,
                              shortaxlabel = TRUE,
                              missdata = FALSE,
                              outgroup = "Abutilon_costicalyx")

writePhylip(datasets4comb,
           file = "filename.phy",
           catalignments = TRUE,
           partitionfile = TRUE)
}

}
\seealso{
\code{\link{catfullGenes}}

\code{\link{catmultGenes}}
}
\author{
Domingos Cardoso, Quezia Cavalcante, and Bruno Vilela
}
